<Window x:Class="CargoWiseNoter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
        xmlns:local="clr-namespace:CargoWiseNoter"
        mc:Ignorable="d"
        Title="MainWindow" Height="50" Width="50" WindowStyle="None" Topmost="True" Background="Transparent" ResizeMode="NoResize" AllowsTransparency="True" ShowInTaskbar="False" Visibility="Hidden" Loaded="Window_Loaded">
    <Window.Resources>
        <Image x:Key="MyImage"
               Source="pack://application:,,,/Assets/icon.png" />
    </Window.Resources>
    <Grid>
        <ui:Button x:Name="LoadNote"
                   Height="50"
                   Width="50"
                   Content="{StaticResource MyImage}"
                   Icon="pack://application:,,,/Assets/icon.png"
                   CornerRadius="25"
                   Click="LoadNote_Click"
                   Appearance="Info"></ui:Button>
        <tray:NotifyIcon x:Name="NotifyIcon"
                         FocusOnLeftClick="True"
                         Icon="pack://application:,,,/Assets/icon.png"
                         MenuOnRightClick="True"
                         TooltipText="Cargowise Noter">
            <tray:NotifyIcon.Menu>
                <ContextMenu>
                    <MenuItem Header="Exit Cargowise Noter" Click="MenuItem_Click"/>
                </ContextMenu>
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>

    </Grid>
</Window>
